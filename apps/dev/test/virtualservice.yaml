apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: test
spec:
  hosts:
  - "localhost"
  gateways:
  - istio-system/test-gateway
  http:
  - match:
    - uri:
        prefix: /test
    route:
    - destination:
        port:
          number: 8080
        host: test
